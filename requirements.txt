# Runtime dependencies — installed in Docker and CI.
# torch is intentionally absent: it is installed separately via the PyTorch
# CPU-only index (https://download.pytorch.org/whl/cpu) in Dockerfile.prod
# and ci.yml before this file is read.
#
# To regenerate the pinned lock file:
#   pip-compile requirements.txt --output-file=requirements.lock
# Run on Linux (matching Dockerfile base image) to get correct wheel hashes.

# Core data
python-dotenv>=1.0
requests>=2.31
pandas>=2.0
numpy>=1.24
scipy>=1.10
tqdm>=4.67.3
pyarrow>=14.0

# ML
scikit-learn>=1.6,<1.7
lightgbm>=4.0
transformers>=4.40
sentence-transformers>=2.2,<3.0
datasets>=2.14

# Retrieval
faiss-cpu>=1.7
rank-bm25>=0.2

# API + observability
fastapi>=0.100
uvicorn[standard]>=0.23
pydantic>=2.0
slowapi>=0.1.9
pydantic-settings>=2.0
prometheus-fastapi-instrumentator>=7.1.0
prometheus-client>=0.19

# LLM
groq>=1.2.0
